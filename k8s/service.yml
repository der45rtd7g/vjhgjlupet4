apiVersion: v1
kind: Service
metadata:
  name: ghhteew
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ghhteew"
    port: 8080
  selector:
    app: ghhteew
